[package]
name = "ns_download_server"
version = "0.1.0"
edition = "2024"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
wildcard_imports = "deny"

[[bin]]
name = "nsdownload-server"
path = "src/main.rs"

[dependencies]
async-trait = "0.1.89"
axum = { version = "0.8.9", features = ["ws"] }
base64 = "0.22"
ns_download_api = { version = "0.1.0", path = "../api", features = ["plugins"] }
ns_download_engine = { version = "0.1.0", path = "../engine", features = ["plugins", "components"] }
fs2 = "0.4.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
thiserror = "2.0.18"
tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros", "sync", "signal", "net", "io-util", "fs", "time"] }
tokio-util = { version = "0.7.18", features = ["io"] }
tower-http = { version = "0.7.0", features = ["fs"] }
utoipa = "5.5.0"
uuid = { version = "1.23.4", features = ["v4"] }

[target.'cfg(windows)'.dependencies]
reqwest = { version = "0.12", features = ["stream", "native-tls", "cookies", "json", "socks"] }

[target.'cfg(unix)'.dependencies]
reqwest = { version = "0.12", default-features = false, features = ["stream", "rustls-tls-native-roots", "cookies", "json", "socks", "charset", "http2", "system-proxy"] }
